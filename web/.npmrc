# Mirrors the repo-root .npmrc so `web/` installs identically whether run from
# here or after a Phase-4 promotion to the repo root.
shamefully-hoist=true
strict-peer-dependencies=false
auto-install-peers=true
