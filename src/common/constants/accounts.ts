export enum AccountStatusType {
  pending = "pending",
  active = "active",
  "pre-migration" = "pre-migration",
  removed = "removed",
}

export enum AccountPlatformType {
  farcaster = "farcaster",
  // lens = "lens",
}
